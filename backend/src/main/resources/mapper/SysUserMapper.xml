<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.experiment2.mapper.SysUserMapper">

    <resultMap id="SysUserMap" type="SysUser">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="email" column="email" />
        <result property="role" column="role" />
        <result property="status" column="status" />
        <result property="createdTime" column="created_time" />
        <result property="updatedTime" column="updated_time" />
    </resultMap>

    <select id="findByUsername" resultMap="SysUserMap">
        SELECT id, username, password, email, role, status, created_time, updated_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="findByEmail" resultMap="SysUserMap">
        SELECT id, username, password, email, role, status, created_time, updated_time
        FROM sys_user
        WHERE email = #{email}
    </select>

    <select id="findById" resultMap="SysUserMap">
        SELECT id, username, password, email, role, status, created_time, updated_time
        FROM sys_user
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, email, role, status)
        VALUES (#{username}, #{password}, #{email}, #{role}, #{status})
    </insert>

    <update id="updatePassword">
        UPDATE sys_user
        SET password = #{password}
        WHERE id = #{id}
    </update>
</mapper>
