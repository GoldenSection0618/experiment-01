<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.experiment2.mapper.UserInfoMapper">

    <resultMap id="UserInfoMap" type="UserInfo">
        <id property="id" column="id" />
        <result property="date" column="date" />
        <result property="name" column="name" />
        <result property="province" column="province" />
        <result property="city" column="city" />
        <result property="address" column="address" />
        <result property="zip" column="zip" />
    </resultMap>

    <select id="findByName" parameterType="string" resultMap="UserInfoMap">
        select
            id,
            date_format(date, '%Y-%m-%d') as date,
            name,
            province,
            city,
            address,
            zip
        from user_info
        where name = #{name}
        limit 1
    </select>

    <select id="findAll" resultMap="UserInfoMap">
        select
            id,
            date_format(date, '%Y-%m-%d') as date,
            name,
            province,
            city,
            address,
            zip
        from user_info
        order by id
    </select>

    <insert id="insert" parameterType="UserInfo" useGeneratedKeys="true" keyProperty="id">
        insert into user_info (date, name, province, city, address, zip)
        values (#{date}, #{name}, #{province}, #{city}, #{address}, #{zip})
    </insert>
</mapper>
